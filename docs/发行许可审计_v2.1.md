# Pulse v2.1 发行许可审计

> 审计日期: 2026-06-23
> 范围: Python 后端依赖、Tauri/Rust 依赖、前端运行时资产、敏感信息与本机配置

## 结论

- 未发现真实 API Key、私钥、支付密钥或本机 PIN 进入发行源代码。
- `backend/config.json`、`data/`、`venv/`、`src-tauri/target/` 已在 `.gitignore` 中，不作为发行源代码提交。
- Rust/Tauri 依赖许可证元数据可解析，`cargo metadata` 覆盖 447 个包，未知许可证数量为 0。
- Python 主依赖均为常见宽松许可证或 Python Software Foundation License。
- `src-tauri/Cargo.toml` 已设置 `publish = false`，降低误发布到 crates.io 的风险。

## Python 主依赖

| 依赖 | 当前版本 | 许可证 |
|---|---:|---|
| aiohttp | 3.14.1 | Apache-2.0 AND MIT |
| aiosqlite | 0.22.1 | MIT |
| fastapi | 0.138.0 | MIT |
| pandas | 3.0.3 | BSD-3-Clause |
| psutil | 7.2.2 | BSD-3-Clause |
| python-dotenv | 1.2.2 | BSD-3-Clause |
| python-multipart | 0.0.32 | Apache |
| pywin32 | 312 | PSF |
| uvicorn | 0.49.0 | BSD-3-Clause |

## Rust/Tauri 依赖

- 元数据来源: `cargo metadata --format-version 1`
- 结果: total=447, unknownCount=0, licenseKinds=28
- 注意: 这是依赖元数据层面的审计，不替代最终安装包内 NOTICE/第三方许可证归档。

## 敏感信息扫描

扫描模式覆盖:

- `sk-*` 风格 API Key
- `api_key` / `secret` / `password` / `token` / `lan_trust_pin`
- 私钥头、AWS/Google/Slack 常见密钥前缀

结论:

- 命中项均为字段名、占位符、测试说明或文档示例。
- 未发现真实凭据进入受版本控制的源码文件。
- E2E 过程中生成的 `data/e2e/config-before-e2e.json` 属于本机备份，应删除或保持在 gitignored `data/` 下，不进入提交。

## 发行许可风险

- 项目自身尚未声明对外发行许可证。如要公开分发源码，应由项目所有者明确选择 `LICENSE`。
- 商店/支付相关代码包含模拟支付路径，发行环境必须继续保持模拟支付和调试验证码默认关闭。
- 若打包二进制对外发布，建议在安装包或发行说明中附带第三方依赖许可证摘要。
