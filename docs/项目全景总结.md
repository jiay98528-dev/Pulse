# PULSE — 项目全景总结

> 编写日期: 2026-06-19 | 最后更新: 2026-06-23
> 用途: 跨会话接力提示词，给另一个 Claude Code 会话快速理解项目全貌
> 当前进度: v2.0 M1-M6 ✅ | v2.1 ✅ | 发行审计 ✅ | E2E 验收 ✅ | 发行就绪

---

## 一、一句话定位

**Surface Go 上的实时数据看板** — v2.0 可配置组件墙仪表盘 + 5 Tab SPA + 模块化主题系统，Tauri v2 + Python Sidecar 架构。构成主义默认主题 (红/黑/白)，CSS 变量驱动主题热插拔。

---

## 二、技术栈

| 层 | 技术 | 选型原因 |
|---|---|---|
| UI 壳 | **Tauri v2** (Rust + WebView2) | Surface Go 4GB RAM，比 Electron 省 100MB+ |
| 前端 | **原生 HTML/CSS/JS** + **Chart.js 4.x** (CDN) | 无框架依赖，轻量，实时图表质量高 |
| 后端 | **Python 3.12+ FastAPI** + WebSocket | psutil/WMI/Deepseek API 只有 Python 版 |
| 数据采集 | **psutil** (系统) + **WMI** (Windows) + **aiohttp** (API) | 已在 collectors/ 封装 |
| 数据库 | **SQLite** (aiosqlite) | 零配置，深度求索历史持久化 |
| 通信 | **WebSocket** (每秒系统 / 每30秒 API) | 实时推送 |
| 图表 | **Chart.js 4.x** | CDN 加载，硬边折线/柱状/环形图 |

---

## 三、项目结构

```
D:\VibeCoding\Pulse\
├── backend/
│   ├── main.py              # FastAPI 入口 + WebSocket + REST API
│   ├── config.py             # 配置读写 (JSON)
│   ├── config.json           # 密钥/限额/设备配置 (已 gitignore)
│   ├── database.py           # SQLite CRUD (usage, balance, devices)
│   ├── requirements.txt      # Python 依赖
│   └── collectors/
│       ├── system.py         # 本机系统采集 (psutil + WMI)
│       ├── deepseek.py       # Deepseek API 采集 (余额 + Token用量)
│       └── wmi_remote.py     # WMI 远程设备采集
├── frontend/
│   ├── index.html            # 5 Tab SPA (仪表盘/硬件/分析/配置/插件)
│   ├── css/style.css         # CSS 变量驱动 + Widget 网格 + 硬件页布局
│   └── js/app.js             # WidgetEngine + WebSocket + 12 图表 + 全部 UI
├── src-tauri/                # Tauri v2 Rust 工程
│   ├── Cargo.toml            # 依赖: tauri + shell + autostart + updater
│   ├── tauri.conf.json       # 窗口: 1280x720, 无边框, 装饰关闭
│   ├── src/main.rs           # Tauri 入口
│   ├── src/lib.rs            # Sidecar 启动+托盘+插件注册
│   ├── capabilities/default.json
│   ├── icons/icon.png        # 托盘图标
│   └── scripts/
│       ├── start-backend.bat # Windows 后端启动脚本
│       └── start-backend.sh  # Unix 后端启动脚本
├── docs/
│   ├── 项目全景总结.md        # ← 本文件
│   ├── 交接文档.md            # 完整项目交接详情
│   ├── 错题本.md              # 设计约束 Checklist + 已知问题
│   ├── Pulse_构成主义设计系统指南.md
│   └── 设计调研_看板布局模式.md
├── PRODUCT.md                # 产品战略层文档
├── DESIGN.md                 # 视觉层设计令牌
├── TODO.md                   # 完成状态跟踪
├── start_pulse.bat           # 纯 Web 模式启动批处理
├── CLAUDE.md                 # 项目元指令
└── .claude/settings.local.json  # 本地权限规则
```

---

## 四、当前完成状态

### ✅ Phase 1: 基础设施 (3/3)
- Git 仓库初始化 + .gitignore
- Memory 持久化 (user/project/design/feedback)
- 代码扫描修复 (StaticFiles 路由排序 + cpu_percent 去重)

### ✅ Phase 2: 运行验证 (3/3)
- API 端点全部正常 (/api/status, /api/config, /api/system/current 等)
- 前端 HTML/CSS/JS 正确加载 (14880 bytes)
- 系统数据实时采集 (CPU/内存/磁盘/网络)

### ✅ Phase 3: Tauri 壳搭建 (3/3)
- src-tauri/ 项目初始化 (Cargo.toml + tauri.conf.json + lib.rs)
- Python Sidecar 自动启停 (CREATE_NO_WINDOW, 退出自动杀进程)
- 系统托盘 (Show/Hide / Open Dashboard / Quit + 左键切换)

### ✅ Phase 4: 功能增强 (3/3)
- **首次启动配置引导** — 检测 API Key 为空时弹出全屏设置覆盖层
- **LAN 设备管理** — 后端 CRUD API + 前端设备列表/添加/编辑/删除/测试连接
- **无边框窗口 + 自定义标题栏** — tauri.conf.json decorations:false + HTML/CSS/JS 标题栏

### ✅ Phase 5: 生产打磨 (3/3)
- **错误处理** — WebSocket 指数退避重连 + 连接中断横幅 + Toast 通知
- **开机自启** — tauri-plugin-autostart (Cargo + lib.rs 注册)
- **自动更新** — tauri-plugin-updater (配置端点 + 插件注册)

### 🔧 Bug 修复历史
- StaticFiles mount 顺序导致 API 404 (移到最后)
- Deepseek 余额解析支持 `balance_infos` 数组格式 (原用 `data.get("balance")`)
- CPU 采集去重 (psutil.cpu_percent 只调一次)

### 🚀 v2.0 里程碑进度

| Milestone | 进度 | 状态 | 关键产出 |
|-----------|------|------|----------|
| **M1** 基础重构 | 5/5 | ✅ | 删除 `/dashboard/usage`、余额字段增强、CSS 变量迁移、5 Tab 重构、系统托盘关闭到托盘 |
| **M2** 仪表盘+硬件 | 6/6 | ✅ | WidgetEngine(S/M/L+拖拽+localStorage)、9 预设组件、硬件页重布局(6 图表+LAN 横栏) |
| **M3** 分析+配置 | 7/7 | ✅ | 4 KPI + 筛选 + 4图表 2x2 + 7列表格 + 对比模式; 3 REST端点 + ZIP导入+去重; 多供应商选择器(Deepseek/OpenAI/Anthropic) + 关于卡片 |
| **M4** 主题系统 | 6/6 | ✅ | ThemeEngine (CSS变量注入+热插拔+图表重绘), 默认theme.json, 外观卡片(下拉+预览), 主题市场(网格浏览), .pulse-theme拖入安装, 主题编辑器(8色+3字体+3字号+导出) |
| **M5** 插件+LAN | 7/7 | ✅ | PluginBase 基类 + PluginManager 发现/加载/生命周期, 插件页UI(卡片+开关+REST API), LAN插件(UDP广播发现+WebSocket配对+弹窗授权), 指标选择器+持久信任(PIN验证+共享指标开关), 硬件页设备横栏(水平卡片+KPI+抽屉详情), 开机自启+自动重连主控端(ReconnectManager+配置UI) |
| **M6** 商店后端 | 5/5 | ✅ | FastAPI 商店+SQLite(独立端口8081, themes/purchases/verification_codes三表), 微信/支付宝商户对接(模拟支付+QR码+15秒自动确认+Webhook签名), 邮箱验证码(6位数字+3分钟有效期+debug模式+恢复已购), 社区GitHub PR自动校验Action(theme.json格式+文件大小<500KB), Pulse前端对接(市场网格+主题详情+购买流程+恢复已购+自动安装) |

**当前关键架构：**
- `WidgetEngine` (app.js) — 可扩展组件系统，注册表 + 渲染 + HTML5 DnD + localStorage
- `WidgetRegistry` — 9 个预设组件 (CPU/内存/磁盘/网络/GPU/余额/Token/缓存/运行时间)
- 硬件页 — 6 张 Chart.js 详情图表 + 状态横幅 + LAN 横栏 + 设备管理
- CSS 变量驱动 — 所有颜色通过 `var(--xxx)` 引用，为主题热插拔做准备

---

## 五、部署环境

### 目标硬件
| 参数 | 值 |
|------|-----|
| 设备 | Surface Go 第一代 |
| CPU | Intel Pentium Gold |
| RAM | 4 GB |
| 屏幕 | 10 英寸, 等效视距 2m (物理 40cm) |
| 交互 | 触屏 (热区 >= 44px) |
| OS | Windows 10 |

### 软件依赖
```
Python 3.12+
Rust (如需编译 Tauri 桌面应用)
Node.js (Tauri CLI 可选)
```

### Python 包 (requirements.txt)
```
fastapi>=0.110.0
uvicorn[standard]>=0.30.0
psutil>=6.0.0
aiohttp>=3.9.0
aiosqlite>=0.20.0
python-dotenv>=1.0.0
pywin32>=305
pandas>=2.0.0
python-multipart>=0.0.9
```

### Tauri 依赖 (Cargo.toml)
```
tauri = { version = "2", features = ["tray-icon"] }
tauri-plugin-shell = "2"
tauri-plugin-autostart = "2"
tauri-plugin-updater = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
```

---

## 六、启动方式

### 纯 Web 模式 (开发调试)
```bash
cd D:/VibeCoding/Pulse
source venv/Scripts/activate
python backend/main.py
# 浏览器打开 http://localhost:8080
```

### Tauri 桌面模式 (需先装 Rust)
```bash
cargo tauri dev    # 开发模式
cargo tauri build  # 打包 .msi
```

---

## 七、设计约束 (关键!)

> 来源: `docs/错题本.md` 和 `DESIGN.md`

1. **不简陋** — 每个几何块是精心构图, 不是省事产物
2. **不浮夸** — 无渐变/无发光/无毛玻璃/无圆滑边框
3. **不赛博** — 1920s 工厂美学, 不是 2049 夜店美学
4. 红/黑/白三色系统占 90%+, 红色永不作大面积背景
5. 直角优先 (`border-radius: 0`)
6. 硬阴影 (`box-shadow` 仅偏移无模糊)
7. 图表最多三色 (红/黑/灰 + 必要时黄)
8. 标题 ALL CAPS, 等宽字体用于数值
9. 最小字号 14px, KPI 数值 56px, 触屏热区 >= 44px

---

## 八、API 端点一览

### 主后端 (端口 8080)
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/health` | 健康检查 (返回 status + configured + plugin_manager) |
| GET | `/api/status` | 服务器 + 配置状态 + 运行时间 |
| GET | `/api/config` | 读取配置 (Key 掩码, 多供应商) |
| POST | `/api/config` | 更新配置 (空 Key 不清空, wmi_remote 类型验证) |
| GET | `/api/system/current` | 最新系统数据快照 |
| GET | `/api/analysis/summary?days=30` | Token 用量汇总 (total_input/output/cached/tokens/cost) |
| GET | `/api/analysis/history?days=30&model=` | 用量历史记录列表 |
| GET | `/api/analysis/models?days=30` | 模型分布统计 |
| POST | `/api/csv/import` | CSV/ZIP 导入 (50MB上限 + 并发控制 + Deepseek透视) |
| GET | `/api/devices` | 列出 LAN 设备 |
| POST | `/api/devices` | 添加设备 |
| PUT | `/api/devices/{id}` | 更新设备 |
| DELETE | `/api/devices/{id}` | 删除设备 |
| POST | `/api/devices/{id}/test` | 测试设备连接 |
| GET | `/api/plugins` | 列出已加载插件及状态 |
| POST | `/api/plugins/{name}/enable` | 启用插件 (广播 plugin_status) |
| POST | `/api/plugins/{name}/disable` | 禁用插件 |
| POST | `/api/lan/discover` | 扫描局域网 Pulse 设备 |
| GET | `/api/lan/devices` | 已配对 LAN 设备列表 |
| POST | `/api/lan/pair-request` | 发起配对请求 |
| POST | `/api/lan/pair-approve` | 批准配对 (PIN验证) |
| WS | `/ws` | WebSocket 实时推送 (system@1s, deepseek@30s, pair_request/success/rejected) |

### Store API (端口 8081)
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/v1/themes` | 主题列表 (type_filter=official/community) |
| GET | `/v1/themes/{id}` | 主题详情 |
| POST | `/v1/themes/{id}/buy` | 创建购买 (模拟支付需SIMULATED_PAYMENTS=1) |
| GET | `/v1/purchases/{id}` | 查询支付状态 |
| POST | `/v1/payments/webhook` | 支付回调 (签名验证) |
| POST | `/v1/restore` | 发送验证码 (DEBUG_RESTORE_CODES=1 启用) |
| POST | `/v1/restore/verify` | 验证码验证 → 返回已购列表 |
| GET | `/api/plugins/{name}/status` | 查询插件状态 |
| GET | `/api/lan/devices` | 搜索到的 LAN 设备列表 |
| WS | `/ws/lan/{device_id}` | LAN 设备配对 WebSocket |

---

## 九、Git 提交历史

```
b0d5f12 fix: 恢复GPT版本+P0修复+数据来源+费用告警+图表增强
2be3b1e feat: 费用限额告警 + Deepseek ZIP兼容
d9b414a fix: Deepseek ZIP CSV格式适配 — utc_date列+pivot透视+cost-only
81290ea fix: 同步P0-P2修复到main分支 — GPT安全版+P0修复全量
2545da8 fix: P0-P2 剩余修复 — 前端+后端+Tauri+商店全量同步
c95e1de feat: M6 商店后端完成 — FastAPI+支付+邮箱验证+前端对接+CI
0c38bf5 feat: M5 完成 — 配对弹窗+指标选择器+自动重连
b3d2e0c feat: M3 分析+配置完成 — 4图表+筛选+对比+多供应商+关于卡片
b2f68ac feat: M4 主题系统 — ThemeEngine + 外观卡片 + 主题市场 + 主题编辑器
dc55e66 feat: M2 仪表盘+硬件页完成 — 组件墙 + 拖拽 + localStorage + 硬件详情
22b7322 feat: M1 基础重构完成 — v2.0 架构基础就绪
10c18f5 init: Pulse dashboard initial commit
```

---

## 十、当前状态

### 已完成
- ✅ v2.0 M1-M6 全部 38 任务完成
- ✅ ChatGPT 5.5 发行安全审计 (CORS/路径穿越/图标/Tauri编译)
- ✅ P0-P2 阻断修复 (配对流程/CSV OOM/autostart/PIN/WS安全)
- ✅ 用户验收改进 (ZIP双CSV导入/费用告警/数据来源/主题弹窗/LAN扫描)
- ✅ Tauri 编译通过 (cargo check 无警告)
- ✅ 文档腐败清除 (TODO/CLAUDE/项目全景/需求规格全量同步)

### v2.1 已完成
- 插件页 LAN 插件卡片增强: 已配对设备列表 + 共享指标配置
- 分析页数据来源列: `source_file` / `imported_at` 持久化与 9 列表格
- 仪表盘 WidgetEngine 图表增强: 内存环图、磁盘柱状图、缓存命中率环图
- 硬件页 LAN 设备抽屉详情: 点击设备卡片展开指标图表与紧凑空态
- 验收证据: `data/e2e/e2e-result.json` 与 `data/e2e/v21-*.png`
- 发行许可审计: `docs/发行许可审计_v2.1.md`

### 遗留问题 (from 错题本)
| ID | 问题 | 状态 |
|----|------|------|
| KNOWN-001 | WMI 远程读取需在主力机配置 DCOM 权限 | 待处理 |
| KNOWN-002 | Deepseek 日用量汇总依赖本地 SQLite 缓存 | 已实现 |
| KNOWN-003 | Surface Go 触屏点击精度 — 已设 44px 热区 | 设计中 |

---

## 十一、会话接力说明

给下一个 Claude Code 会话的 prompt 示例:

```
你正在接手 Pulse 项目。先读取 docs/项目全景总结.md 了解全貌。
关键操作:
1. 启动: source venv/Scripts/activate && python backend/main.py
2. 编译 Tauri: cargo tauri build (需 Rust)
3. 设计约束: 过错题本.md 的 checklist
4. 配置文件: backend/config.json (已 gitignore, 安全存储 Key)
```

---

*本文档由 Claude Code 自动生成，保持与项目实际状态同步。*
